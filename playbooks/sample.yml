---
  - name: "Playing with Ansible and Git"
    hosts: localhost
    connection: local 
    tasks:

    - name: "just execute a ls -lrt command"
      shell: "cat /home/madhuboyina/work/git/ansible/group_vars/bb_config/bbexporter_config.yml"
      register: "output"

    - debug: var=output.stdout_lines
